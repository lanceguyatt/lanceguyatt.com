---
import { type SiteProps, SITE } from '@/config'
import { BaseHead, SchemaWebPage } from '@/components/index.astro'
import '@/styles/workbench.css'

export type DefaultProps = SiteProps & {
  class?: string
  theme?: string
}

const {
  title,
  name,
  description,
  url,
  image,
  theme,
  class: className
} = Astro.props as DefaultProps
const meta = { name, title, description, url, image }
---

<html lang={SITE.lang}>
  <head>
    <BaseHead {...meta}>
      <slot name="head" />
    </BaseHead>

    <!-- <script src="//unpkg.com/alpinejs" defer></script> -->
  </head>
  <body class:list={['bg-background', 'text-foreground', className, theme]}>
    <slot name="header" />
    <slot />
    <slot name="footer" />
    <SchemaWebPage {...meta} />
  </body>
</html>
