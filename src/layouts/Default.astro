---
import { ISITE, SITE } from '~/config'
import { BaseHead, SchemaWebPage } from '~/components/index.astro'

// import 'uno:base.css'
import '~/styles/workbench.css'

export interface IDefault extends ISITE {
  class?: string
}

const {
  title,
  name,
  description,
  url,
  image,
  class: className
} = Astro.props as IDefault
const meta = { name, title, description, url, image }
---

<html lang={SITE.lang}>
  <BaseHead {...meta}>
    <slot name="head" />
  </BaseHead>
  <body class:list={[className]}>
    <slot name="header" />
    <main class="flex-auto">
      <slot />
    </main>
    <slot name="footer" />
    <SchemaWebPage {...meta} />
  </body>
</html>
