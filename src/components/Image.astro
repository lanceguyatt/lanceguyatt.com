---
export interface ImageProps {
  src: string
  alt: string
  loading?: string
}

const { src, alt, loading = 'lazy', ...props } = Astro.props as ImageProps
---

<img
  class="border-img-bar object-cover p-2px"
  src={src}
  alt={alt}
  loading={loading}
  {...props}
/>
