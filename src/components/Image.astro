---
export interface IImage {
  src: string
  alt: string
  loading?: 'lazy' | 'eager'
}

const { src, alt, loading = 'lazy', ...props } = Astro.props as IImage
---

<img
  src={src}
  alt={alt}
  loading={loading}
  class="border-img-bar object-cover p-2px"
  {...props}
/>
